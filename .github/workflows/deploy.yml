name: Déploiement GitHub Pages

on:
  push:
    branches:
      - main  # Ou master si ta branche s'appelle master
  workflow_dispatch:

# Autorise le script à publier le site sur GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le code
        uses: actions/checkout@v4

      - name: Configurer GitHub Pages
        uses: actions/configure-pages@v5

      - name: Préparer les fichiers HTML/CSS
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'  # Déploie le dossier courant (où se trouve index.html)

      - name: Déployer sur GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
